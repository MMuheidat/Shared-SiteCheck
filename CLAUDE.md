# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The entire project lives in the `sitecheck/` subdirectory — the repo root contains nothing else. Run all `npm`/`npx prisma` commands from `sitecheck/`. The live SQLite DB is `sitecheck/prisma/dev.db`; `sitecheck/dev.db` is a stale copy.

Full project documentation, commands, architecture, and gotchas:

@sitecheck/CLAUDE.md
